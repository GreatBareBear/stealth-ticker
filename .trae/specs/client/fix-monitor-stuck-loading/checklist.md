- [x] 无论用户添加带前缀（`sh601857`）还是不带前缀（`601857`）的代码，行情悬浮窗口均能正确获取并显示中国石油的实时行情。
- [x] 后台轮询遇到 GBK 解码异常或网络抖动时，不会停止数据刷新，`scheduleNextPoll()` 会一直被调用。
- [x] 美股等字段数较少的股票（`< 35`）不会被错误丢弃，同样能正常显示。
- [x] 运行 `npm run typecheck:web` 与 `npm run typecheck:node` 均无报错。