- [x] 自选股票的表格“操作”列中，预警图标（BellOutlined）与删除图标（DeleteOutlined）并排摆放。
- [x] 点击预警图标弹出的设置窗口中，支持配置“价格预警”（精度保留两位小数）或“涨跌幅预警”（±0.1%–±20%，步长0.1%），并能选择“高于”或“低于”。
- [x] 提醒文案输入框限制最多 50 汉字，并且在触发时正确将 `${股票名称}`、`${价格}`、`${阈值}` 替换为实时数据。若变量缺失，会使用默认或降级文本提示。
- [x] 支持三选一的提醒方式：弹窗（屏幕右下角弹出，3秒后自动消失）、系统提示音、托盘图标闪烁（持续 10 秒）。
- [x] 悬浮窗每次轮询获取新行情数据时（延迟≤1秒，受刷新频率控制），若股票触发了配置的预警条件，能成功发起预警动作。
- [x] 触发过一次预警后，在条件没有重置（如跌回阈值内或重启应用前），不会在下一次轮询时被重复疯狂提醒。