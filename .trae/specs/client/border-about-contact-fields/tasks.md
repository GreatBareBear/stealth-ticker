# Tasks

- [x] Task 1: 为“关于”页面的可复制联系方式增加输入框风格边框
  - [x] SubTask 1.1: 在 `client/src/renderer/src/pages/About.tsx` 中定位 Home / E-mail / QQ 群三条信息的值展示区域（`Text copyable`）。
  - [x] SubTask 1.2: 为三条信息的值外层增加统一的边框容器样式（边框、圆角、内边距、背景），使其视觉接近只读输入框。
  - [x] SubTask 1.3: 确保复制图标仍可点击，且布局不出现横向溢出或新增滚动条。

# Task Dependencies
无
