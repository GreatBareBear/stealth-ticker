- [x] 免打扰入口被完全重构为标准 `<Button>`，不再使用 `<Tag>`。
- [x] “关闭”二字被移除，未开启时仅显示“免打扰”。
- [x] 临时暂停生效时，按钮呈现 `danger` 的红色/橙色强警示状态。
- [x] 时间段生效时，按钮呈现 `primary` 的蓝色高亮状态。
- [x] 冗余的箭头图标和 Tooltip 被移除。
- [x] 运行 `npm run typecheck:web` 没有 TypeScript 错误。
