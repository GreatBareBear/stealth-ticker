# Tasks

- [x] Task 1: 在全局 ConfigProvider 中修改 Segmented 样式
  - [x] SubTask 1.1: 在 `client/src/renderer/src/pages/Settings.tsx` 中找到 `ConfigProvider` 的 `components` 配置对象。
  - [x] SubTask 1.2: 添加 `Segmented` 的定制 token。
  - [x] SubTask 1.3: 为 `Segmented` 设置 `itemSelectedBg`（例如纯白色伴随深阴影，或主色浅背景 `#e6f4ff`），并设置 `itemSelectedColor`（例如主色 `#1677ff`），以提升选中项的对比度。
  - [x] SubTask 1.4: 适当调整 `trackBg`（未选中时的轨道背景，比如略微加深一点到 `rgba(0,0,0,0.06)`）以反衬选中项的明亮。

# Task Dependencies
- [Task 1] 无前置依赖。