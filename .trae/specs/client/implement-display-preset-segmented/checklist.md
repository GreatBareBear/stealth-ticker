- [x] DisplayTab 预设区域从三个按钮改为 Segmented 模式选择器（推荐/隐蔽/清晰），并能显示自定义状态。
- [x] 选择任意预设后会应用对应字段，并持久化 `settings.displayPreset`。
- [x] 用户手动修改预设相关字段后，会自动切换为自定义状态（避免误导）。
- [x] 预设选择器下方存在一行极简差异说明，帮助用户理解模式差别。
- [x] `npm run typecheck:web` 通过。
