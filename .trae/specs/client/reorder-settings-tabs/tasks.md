# Tasks

- [x] Task 1: 调整设置界面 Tab 顺序和命名
  - [x] SubTask 1.1: 在 `client/src/renderer/src/pages/Settings.tsx` 中，找到 `const items = [...]` 定义。
  - [x] SubTask 1.2: 按照新顺序重新排列 `items` 数组的元素：
    1. 自选股票
    2. 显示
    3. 高级
    4. 股价图
    5. 数据看板
    6. 会员
    7. 其它
  - [x] SubTask 1.3: 将原先的“其他”重命名为“其它”，并将各元素的 `key` 按照顺序重新编号（'1' 到 '7'）。

# Task Dependencies
无
